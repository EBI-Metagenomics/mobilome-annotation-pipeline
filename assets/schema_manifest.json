{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/ebi-metagenomics/mobilome-annotation-pipeline/main/assets/schema_manifest.json",
    "title": "ebi-metagenomics/mobilome-annotation-pipeline pipeline - params.annotation_manifest schema",
    "description": "Schema for the annotation manifest provided with --annotation_manifest. Each row maps a sample to pre-computed annotation files from the genomes-catalogue-pipeline. All annotation columns are optional.",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces. Must match the sample column in the input samplesheet.",
                "meta": ["id"],
                "uniqueItems": true
            },
            "amrfinder_tsv": {
                "type": "string",
                "pattern": "^\\S+\\.tsv(\\.gz)?$",
                "errorMessage": "AMRFinderPlus TSV output (optionally compressed with .gz). Column layout differs from the MAP-internal run and is normalised automatically."
            },
            "antismash_gff": {
                "type": "string",
                "pattern": "^\\S+\\.gff(\\.gz)?$",
                "errorMessage": "antiSMASH GFF output from mgnify-pipelines-toolkit/antismash_gff_builder script (optionally compressed with .gz)."
            },
            "gecco_gff": {
                "type": "string",
                "pattern": "^\\S+\\.gff(\\.gz)?$",
                "errorMessage": "GECCO GFF output using convert (optionally compressed with .gz)."
            },
            "sanntis_gff": {
                "type": "string",
                "pattern": "^\\S+\\.gff(\\.gz)?$",
                "errorMessage": "SanntiS GFF output (optionally compressed with .gz)."
            }
        },
        "required": ["sample"]
    }
}
