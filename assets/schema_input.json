{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/ebi-metagenomics/mobilome-annotation-pipeline/master/assets/schema_input.json",
    "title": "ebi-metagenomics/mobilome-annotation-pipeline pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"],
		        "uniqueItems": true
            },
            "assembly": {
                "type": "string",
                "pattern": "^\\S+\\.(fa|fna|fasta)(\\.gz)?$",
                "errorMessage": "The assembly needs to be a fasta file (optionally compressed with .gz)"
            },
            "user_proteins_gff": {
                "type": "string",
                "pattern": "^\\S+\\.(gff)(\\.gz)?$",
		"errorMessage": "The file with proteins detected for the assembly needs to be a gff3 file (optionally compressed with .gz)"
            },
            "virify_gff": {
		"type": "string",
                "pattern": "^\\S+\\.gff$",
                "errorMessage": "The VIRify file generated for the assembly needs to be a gff3 file",
            }
        },
        "required": ["sample", "assembly"]
    }
}
