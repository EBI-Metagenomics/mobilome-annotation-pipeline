// Enable module binaries feature for scripts in resources/usr/bin
nextflow.enable.moduleBinaries = true

process {
    withName: PATHOFACT2_TOXINS {
        ext.args = [
            "-k", "5"
        ].join(' ')
    }
}
